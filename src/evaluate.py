"""
Evaluation module.

Responsibility: compare candidate models using the defined metrics
(MAE primary, RMSE and MAPE secondary — see PROJECT_PLAN.md) against
the baseline (median price per neighbourhood) and select the final
model.

To be implemented in Phase 5.
"""

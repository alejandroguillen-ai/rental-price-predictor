"""
Prediction API (FastAPI).

Responsibility: expose the trained model via endpoints:
- GET /health -> service health check
- POST /predict -> receives listing characteristics and returns the
  predicted price per night

To be implemented in Phase 6.
"""
